/* Retired: v1 created an independent scroll shell and is intentionally inert. */
(function(global){
  'use strict';
  global.IGDCMediaPlaybackShell = { VERSION:'retired-v1', active:false };
})(window);

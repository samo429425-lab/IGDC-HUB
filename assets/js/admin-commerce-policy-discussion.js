/* IGDC administrator policy discussion workspace v1.0.0 */
(function(){
  'use strict';
  var CONTROL='/.netlify/functions/commerce-country-control';
  var $=function(id){return document.getElementById(id);};
  var text=function(v){return String(v==null?'':v).trim();};
  var esc=function(v){return String(v==null?'':v).replace(/[&<>"']/g,function(ch){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[ch];});};
  var token='',sessionPromise=null,currentScope=null,currentWorkspace=null,currentResponse=null;
  var TOKEN_KEYS=['osauth.tokens.v2','osauth.tokens.v1','igdc.tokens','igdc_auth_tokens','auth0_tokens','auth0spa','igdc_id_token','id_token','auth0_id_token'];

  function jwt(v){var t=text(v);return t.split('.').length===3&&t.length>32?t:'';}
  function parse(v){try{return JSON.parse(v);}catch(_e){return null;}}
  function pushToken(v,out,seen,depth){if(depth>4||v==null)return;if(typeof v==='string'){var t=jwt(v);if(t&&!seen[t]){seen[t]=1;out.push(t);return;}var p=parse(v);if(p)pushToken(p,out,seen,depth+1);return;}if(Array.isArray(v)){v.forEach(function(x){pushToken(x,out,seen,depth+1);});return;}if(typeof v==='object'){['id_token','idToken','access_token','accessToken','token','__raw','raw'].forEach(function(k){pushToken(v[k],out,seen,depth+1);});}}
  async function tokens(){var out=[],seen={},tasks=[];[window,window.parent,window.top].forEach(function(w){try{if(!w||tasks.some(function(x){return x.w===w;}))return;void w.location.href;var box={w:w};tasks.push(box);if(w.IGDCMemberAuth&&w.IGDCMemberAuth.getIdToken)box.p=Promise.resolve(w.IGDCMemberAuth.getIdToken()).then(function(v){pushToken(v,out,seen,0);}).catch(function(){});[w.localStorage,w.sessionStorage].forEach(function(store){if(!store)return;TOKEN_KEYS.forEach(function(k){try{pushToken(store.getItem(k),out,seen,0);}catch(_e){}});});}catch(_e){}});await Promise.all(tasks.map(function(x){return x.p||Promise.resolve();}));return out;}
  async function request(action,method,params,body,authToken){var u=new URL(CONTROL,location.origin);u.searchParams.set('action',action);Object.keys(params||{}).forEach(function(k){if(params[k]!=null&&params[k]!=='')u.searchParams.set(k,params[k]);});var init={method:method||'GET',credentials:'same-origin',cache:'no-store',headers:{Accept:'application/json',Authorization:'Bearer '+authToken}};if(init.method!=='GET'){init.headers['Content-Type']='application/json';init.body=JSON.stringify(Object.assign({action:action},body||{}));}var res=await fetch(u.pathname+u.search,init),data=await res.json().catch(function(){return null;});if(!res.ok||!data||data.ok!==true){var e=new Error(data&&data.error||('HTTP '+res.status));e.status=res.status;throw e;}return data;}
  async function ensure(){if(token)return token;if(sessionPromise)return sessionPromise;sessionPromise=(async function(){var list=await tokens();for(var i=0;i<list.length;i++){try{await request('session','GET',{},null,list[i]);token=list[i];return token;}catch(_e){}}throw new Error('관리자 공통 세션을 확인하지 못했습니다.');})();try{return await sessionPromise;}finally{sessionPromise=null;}}
  async function api(action,method,params,body){var t=await ensure();try{return await request(action,method,params,body,t);}catch(e){if(Number(e.status)===401){token='';t=await ensure();return request(action,method,params,body,t);}throw e;}}

  function lines(value){var out=[];text(value).split(/\r?\n/).map(function(v){return v.trim();}).filter(Boolean).forEach(function(v){if(out.indexOf(v)<0)out.push(v);});return out;}
  function scopeState(){var s=window.IGDC_ADMIN_COUNTRY_SCOPE||{};return{country:text(s.country).toUpperCase(),region:text(s.region||'NATIONWIDE').toUpperCase()||'NATIONWIDE',regionGroup:text(s.regionGroup)};}
  function buildScope(type){var s=scopeState();if(type==='global')return{scopeType:'global'};if(type==='regional'){if(!s.regionGroup)throw new Error('먼저 권역을 선택해 주세요.');return{scopeType:'regional',regionGroup:s.regionGroup};}if(!s.country)throw new Error('먼저 국가를 선택해 주세요.');return{scopeType:'country',regionGroup:s.regionGroup,countryCode:s.country,subdivisionCode:s.region};}
  function modalNotice(message,kind){var el=$('policyModalNotice');el.className='notice '+(kind==='ok'?'ok':kind==='warn'?'warn':kind==='fail'?'fail':'');el.textContent=message;el.classList.remove('hidden');}
  function clearModalNotice(){var el=$('policyModalNotice');el.classList.add('hidden');el.textContent='';}
  function workspaceStatus(workspace){if(!workspace)return'저장 정책 없음';var label=workspace.status==='active'?'활성':workspace.status==='paused'?'일시중지':'초안';return label+(workspace.validUntil?' · '+workspace.validUntil.slice(0,10)+'까지':'')+(workspace.updatedAt?' · '+workspace.updatedAt.slice(0,16).replace('T',' '):'');}
  function updateBadge(scope,workspace){var id=scope.scopeType==='global'?'globalPolicyState':scope.scopeType==='regional'?'regionalPolicyState':'countryPolicyState',el=$(id);if(el)el.textContent='관리자 정책: '+workspaceStatus(workspace);}
  function renderMessages(messages){var el=$('policyTranscript'),rows=Array.isArray(messages)?messages:[];if(!rows.length){el.innerHTML='<div class="small">저장된 협의 기록이 없습니다.</div>';return;}el.innerHTML=rows.map(function(row){var role=row.role==='assistant'?'assistant':'user';return'<div class="policy-message '+role+'"><strong>'+(role==='assistant'?'AI':'관리자')+'</strong>\n'+esc(row.content||'')+'</div>';}).join('');el.scrollTop=el.scrollHeight;}
  function proposalData(workspace){return workspace&&workspace.latestProposal&&typeof workspace.latestProposal==='object'?workspace.latestProposal:{};}
  function renderWorkspace(workspace){currentWorkspace=workspace;var proposal=proposalData(workspace);$('policyScopeLabel').textContent=(workspace.scope&&workspace.scope.scopeLabel)||workspace.scope&&workspace.scope.scopeType||'';$('policyInstruction').value=workspace.administratorInstruction||'';$('policyFinalDecision').value=workspace.finalDecision||'';$('policyStatus').value=workspace.status||'draft';$('policyValidityDays').value=workspace.validityDays||30;$('policyWeights').value=JSON.stringify(workspace.categoryWeights&&Object.keys(workspace.categoryWeights).length?workspace.categoryWeights:(proposal.categoryWeights||{}),null,2);$('policyPriorityDirections').value=(workspace.priorityDirections&&workspace.priorityDirections.length?workspace.priorityDirections:(proposal.priorityDirections||[])).join('\n');$('policyAvoidDirections').value=(workspace.avoidDirections&&workspace.avoidDirections.length?workspace.avoidDirections:(proposal.avoidDirections||[])).join('\n');$('policyPriorityTargets').value=(workspace.manualPriorityTargets&&workspace.manualPriorityTargets.length?workspace.manualPriorityTargets:(proposal.manualPriorityTargets||[])).join('\n');$('policyBlockedTargets').value=(workspace.manualBlockedTargets&&workspace.manualBlockedTargets.length?workspace.manualBlockedTargets:(proposal.manualBlockedTargets||[])).join('\n');$('policyProposal').textContent=Object.keys(proposal).length?JSON.stringify(proposal,null,2):'AI와 협의하면 구조화된 정책안이 표시됩니다.';renderMessages(workspace.messages||[]);$('policyCountryFields').classList.toggle('hidden',workspace.scope&&workspace.scope.scopeType!=='country');updateBadge(workspace.scope,workspace);}
  async function openPolicy(type){clearModalNotice();try{currentScope=buildScope(type);}catch(e){var n=$('notice');if(n){n.className='notice warn';n.textContent=e.message;n.classList.remove('hidden');}return;}$('policyModal').classList.remove('hidden');$('policyModalTitle').textContent=type==='global'?'전 세계 통합 정책·AI 협의':type==='regional'?'선택 권역 정책·AI 협의':'선택 국가 정책·수동 통제';$('policyScopeLabel').textContent='정책 작업공간을 불러오는 중입니다.';try{var data=await api('policy_workspace','GET',currentScope);currentResponse=data;renderWorkspace(data.workspace);}catch(e){modalNotice(e.message||'정책 작업공간을 불러오지 못했습니다.','fail');}}
  function closePolicy(){$('policyModal').classList.add('hidden');clearModalNotice();}
  function setBusy(busy){['policyDiscussBtn','policySaveBtn','policyCloseBtn'].forEach(function(id){var el=$(id);if(el)el.disabled=!!busy;});}
  async function discuss(){clearModalNotice();var instruction=text($('policyInstruction').value);if(!instruction){modalNotice('AI에 전달할 정책 지시나 질문을 입력해 주세요.','warn');return;}setBusy(true);try{var data=await api('policy_ai_discuss','POST',{}, {scope:currentScope,instruction:instruction});currentResponse=data;renderWorkspace(data.workspace);var p=data.ai&&data.ai.proposal||{};$('policyWeights').value=JSON.stringify(p.categoryWeights||{},null,2);$('policyPriorityDirections').value=(p.priorityDirections||[]).join('\n');$('policyAvoidDirections').value=(p.avoidDirections||[]).join('\n');if(currentScope.scopeType==='country'){$('policyPriorityTargets').value=(p.manualPriorityTargets||[]).join('\n');$('policyBlockedTargets').value=(p.manualBlockedTargets||[]).join('\n');}$('policyProposal').textContent=JSON.stringify(p,null,2);modalNotice(data.ai&&data.ai.error?'AI 협의는 제한 모드로 저장됐습니다: '+data.ai.error:'AI 정책안을 작성하고 협의 기록을 저장했습니다. 관리자 최종 결정을 확인해 주세요.',data.ai&&data.ai.error?'warn':'ok');}catch(e){modalNotice(e.message||'AI 정책 협의에 실패했습니다.','fail');}finally{setBusy(false);}}
  function weightsValue(){var raw=text($('policyWeights').value);if(!raw)return{};var parsed=parse(raw);if(!parsed||typeof parsed!=='object'||Array.isArray(parsed))throw new Error('품목군 가중치 JSON 형식을 확인해 주세요.');return parsed;}
  async function save(){clearModalNotice();var payload;try{payload={scope:currentScope,administratorInstruction:text($('policyInstruction').value),finalDecision:text($('policyFinalDecision').value),status:$('policyStatus').value,validityDays:Number($('policyValidityDays').value)||30,categoryWeights:weightsValue(),priorityDirections:lines($('policyPriorityDirections').value),avoidDirections:lines($('policyAvoidDirections').value),manualPriorityTargets:lines($('policyPriorityTargets').value),manualBlockedTargets:lines($('policyBlockedTargets').value)};}catch(e){modalNotice(e.message,'warn');return;}setBusy(true);try{var data=await api('policy_decision_save','POST',{},payload);currentResponse=data;renderWorkspace(data.workspace);modalNotice('관리자 결정과 정책 통제 내용을 저장했습니다. 활성 정책은 다음 자동 수집부터 일반 AI보다 우선 적용됩니다.','ok');}catch(e){modalNotice(e.message||'관리자 정책 저장에 실패했습니다.','fail');}finally{setBusy(false);}}
  function download(){var data=currentResponse||{ok:true,workspace:currentWorkspace};if(!data)return;var scope=currentScope||{},stamp=new Date().toISOString().replace(/[:.]/g,'-'),name='IGDC_POLICY_'+text(scope.scopeType).toUpperCase()+(scope.regionGroup?'_'+text(scope.regionGroup).toUpperCase():'')+(scope.countryCode?'_'+scope.countryCode:'')+'_'+stamp+'.json',blob=new Blob([JSON.stringify(data,null,2)],{type:'application/json;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(url);},1500);}
  function wire(){if(!$('policyModal'))return;$('globalPolicyBtn').addEventListener('click',function(){openPolicy('global');});$('regionalPolicyBtn').addEventListener('click',function(){openPolicy('regional');});$('countryPolicyBtn').addEventListener('click',function(){openPolicy('country');});$('policyCloseBtn').addEventListener('click',closePolicy);$('policyDiscussBtn').addEventListener('click',discuss);$('policySaveBtn').addEventListener('click',save);$('policyDownloadBtn').addEventListener('click',download);$('policyModal').addEventListener('click',function(e){if(e.target===$('policyModal'))closePolicy();});document.addEventListener('keydown',function(e){if(e.key==='Escape'&&!$('policyModal').classList.contains('hidden'))closePolicy();});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire();
})();

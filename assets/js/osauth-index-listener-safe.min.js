/*! osauth-index-listener-safe.min.js (cleaned) */
(function(){
  'use strict';
  window.getUserRole = function(){
    try {
      return localStorage.getItem('user_role') || 'guest';
    } catch(_) { return 'guest'; }
  };
})();